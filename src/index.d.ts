/**
	* Validates whether a given string contains utf8 characters.
    *
    * @param input The string to validate
    * @returns {boolean} Whether or not the string is valid utf8
*/
export function utf8_validate(input: string): boolean;